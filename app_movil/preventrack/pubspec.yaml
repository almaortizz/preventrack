name: preventrack
description: "Sistema de Preventa y Distribución."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  shared_preferences: ^2.5.0
  provider: ^6.1.0
  intl: ^0.20.0

  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^14.0.3
  sqflite: ^2.4.4
  path: ^1.9.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png